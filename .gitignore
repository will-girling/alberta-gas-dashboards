# Raw downloads and intermediates. Everything here is reproducible by
# running the download and prepare scripts, and most of it is far too
# large for a repository.
petrinex_raw/
gdsr/
csr_raw/
outages_raw/
westcoast_raw/
ST_37_Wells.gdb/
Pipelines_SHP/
Pipeline_Installations_SHP/
*.gdb/

# Well-month parquets: 128 MB and 113 MB, over GitHub's file limit.
# processed/deploy/ carries the precomputed outputs instead.
processed/ab_well_production.parquet
processed/ab_well_production_located.parquet
processed/major_operating_gas_pipelines.geojson
processed/ngtl_operating_pipelines.geojson
processed/bc_transmission_pipelines.geojson
processed/ngtl_meter_stations.geojson

# Logs and local artifacts
*.log
refresh_log.json
*_download_log.json
node_modules/
__pycache__/
.DS_Store
.playwright_profile/
.playwright_profile_csr/
*.pdf
~$*
.~lock.*

# BCER bulk downloads. prod_csv.zip is 117 MB - over GitHub's 100 MB
# hard limit, not just the warning threshold. Both are reproducible:
#   curl -O https://iris.bcogc.ca/download/prod_csv.zip
#   curl -O https://iris.bcogc.ca/download/well_index.csv
prod_csv.zip
well_index.csv

# Intermediates built by the prepare scripts and read by nothing at
# runtime. ab_gas_history_play.parquet alone is 91 MB.
processed/ab_gas_history_play.parquet
processed/bc_well_production.parquet
processed/bc_well_locations.parquet
processed/bc_frac_activity.parquet
processed/plant_c5_12m_by_play.parquet
processed/dls_township_centroids.parquet
processed/ab_history_located.parquet

# NOTE: processed/monitor/ and processed/ngtl_outage_history.csv ARE
# committed - they are small and the deployed apps read them directly.
# The outage history in particular cannot be rebuilt on Streamlit Cloud
# because outages_raw/ is ignored, so the built CSV must travel.
